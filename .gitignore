# Segredos — NUNCA comittar
.env
.env.*
!.env.example
*.pem
*.key
# Certificados digitais (e-CNPJ A1 — chave privada que assina NF-e)
*.pfx
*.p12
# Planilhas com dados fiscais/cadastrais sensíveis
DROPCHINA.xlsx
*senha*

# Tokens e auth da Shopify CLI
.shopify/
.shopifyrc

# Node
node_modules/
*.log
npm-debug.log*

# Backups e temporários
*.tmp.*
*.swp
*.swo
*~
.DS_Store
*:Zone.Identifier

# Theme exports brutos (zips)
*.zip

# Exports de produtos ML/Bling (gerados, não versionar)
produtos_*.csv

# Editor / IDE
.vscode/
.idea/
*.sublime-*

# OS
Thumbs.db
ehthumbs.db
Desktop.ini

# App scaffold criado por shopify app init (sub-repo independente, não usado neste projeto)
dropchina-setup/

# Arquivo placeholder vazio
index.html

# Banners staging (banners originais do user, antes de copiar pra theme/assets)
banner*.png

# MCP local (cwd absoluto, específico da máquina)
.mcp.json
